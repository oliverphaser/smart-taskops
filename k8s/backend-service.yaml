apiVersion: v1
kind: Service
metadata:
  name: task-service
  namespace: smart-taskops
spec:
  selector:
    app: task-service
  ports:
    - port: 4001
      targetPort: 4001
      protocol: TCP
  type: ClusterIP
